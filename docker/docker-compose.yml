version: "3.8"

services:
  meal-planner:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ../data:/data
    restart: unless-stopped
